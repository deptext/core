# Nix build outputs
result
result-*

# Nix flake lock (keep tracked, but ignore local modifications)
# flake.lock is intentionally tracked for reproducibility

# Persisted bloom outputs (generated by nix build)
# These are created alongside seed.nix files when persist=true
# Use ** to match anywhere, but exclude lib/ source code
**/stats/
**/package-download/
**/source-download/
!lib/processors/package-download/
!lib/processors/source-download/

# Editor and IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.log

# Environment files (if any scripts use them)
.env
.env.local
